/**
 * <%= moduleName %>
 */
import React from 'react';
import { View } from 'react-native';

const <%= moduleName %> = () => {
    return (
        <View>
            <%= moduleName %> component
        </View>
    );
};

export default <%= moduleName %>;
