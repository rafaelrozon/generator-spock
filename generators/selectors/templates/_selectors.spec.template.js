/**
 * <%= moduleName %> Selectors Spec
 */
import * as Selectors from './selectors';

describe("<%= moduleName %> tests", function() {

    it("does stuff", function() {
        expect(true).to.equal(false);
    });

});
