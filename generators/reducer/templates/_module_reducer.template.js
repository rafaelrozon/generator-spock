/**
 * <%= moduleName %> Reducer
 */
import * as Types from './<%= typesFilename %>';

export default (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
